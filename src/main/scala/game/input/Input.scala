package game.input

// Define an object named "Input" to track key presses
// Initially set to "false" as no keys are pressed at the start
object Input {
  // Tracks whether the "W" key is currently pressed
  var wPressed = false

  // Tracks whether the "A" key is currently pressed
  var aPressed = false

  // Tracks whether the "S" key is currently pressed
  var sPressed = false

  // Tracks whether the "D" key is currently pressed
  var dPressed = false

  // Tracks whether the "Q" key is currently pressed
  var qPressed = false

  // Tracks whether the "Space" key is currently pressed
  var spacePressed = false
}


